<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Platform Academy</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; background: #0f172a; color: #e2e8f0; min-height: 100vh; padding: 2rem; }
    .container { max-width: 800px; margin: 0 auto; }
    h1 { font-size: 2rem; margin-bottom: 0.5rem; color: #38bdf8; }
    .subtitle { color: #94a3b8; margin-bottom: 2rem; }
    .status { display: inline-block; padding: 0.25rem 0.75rem; border-radius: 9999px; font-size: 0.875rem; margin-bottom: 2rem; }
    .status.connected { background: #064e3b; color: #34d399; }
    .status.disconnected { background: #7f1d1d; color: #fca5a5; }
    .card { background: #1e293b; border: 1px solid #334155; border-radius: 0.5rem; padding: 1.5rem; margin-bottom: 1rem; }
    .card h3 { color: #38bdf8; margin-bottom: 0.5rem; }
    .card p { color: #94a3b8; }
    .empty { text-align: center; padding: 3rem; color: #64748b; }
  </style>
</head>
<body>
  <div class="container">
    <h1>Platform Academy Dashboard</h1>
    <p class="subtitle">Technologist Academy - Platform Engineering Workshop</p>
    <p>Backend API: <span class="status <%= backendStatus %>"><%= backendStatus %></span></p>

    <% if (services.length > 0) { %>
      <% services.forEach(service => { %>
        <div class="card">
          <h3><%= service.name %></h3>
          <p><%= service.description %></p>
          <p style="margin-top: 0.5rem; font-size: 0.875rem; color: #64748b;">Status: <%= service.status %></p>
        </div>
      <% }); %>
    <% } else { %>
      <div class="card empty">
        <p>No services loaded. The backend API is not connected.</p>
        <p style="margin-top: 0.5rem;">Start the backend container and set BACKEND_URL to connect.</p>
      </div>
    <% } %>
  </div>
</body>
</html>
